"""
Paid API Tests

⚠️  WARNING: Tests in this directory use real external APIs and WILL INCUR CHARGES!

These tests should only be run manually when you need to verify that the system
works with actual data providers. They are excluded from automated test runs
to prevent unexpected API charges.

To run these tests:
    ./run_paid_api_tests.sh
    # or
    pytest -m paid_api

All tests in this directory should be marked with @pytest.mark.paid_api
"""
